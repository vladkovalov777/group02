[project]
name = "projest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "logtail-python>=0.3.4",
    "python-dotenv>=1.1.1",
    "redis>=6.4.0",
]
